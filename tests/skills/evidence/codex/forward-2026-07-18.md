# Codex forward test — vNext 5-skill 체계 (2026-07-18)

- Runner: codex companion task, model `gpt-5.6-luna`, effort `low`, fresh thread per run, read-only(no --write)
- Tree: 승격 완료된 .agents/skills (uncommitted worktree, base 7deea45)
- 규약: 기대 정답·rubric 비누설, 자연 요청만 제공. 스킬 선택은 transcript의 파일 read 로그로 관찰.

| Run | Prompt(요약) | 기대 | 관찰 | 판정 |
|---|---|---|---|---|
| F1 | S01 단일 분류 Agent (baseline 동일 문구) | af-discover-assets, Workflow 미생성 | `af-discover-assets/SKILL.md` read 확인. 결론 "최소 구성 = 분류 Agent + 기준 Resource; Workflow는 조건부". Resource/Dependency 비자산 분리 정확 | **PASS** — baseline PARTIAL FAIL(Workflow 과잉) 대비 개선 입증 |
| F2 | S03 Agent-선택 MCP Tool (baseline 동일 문구) | Invocation Control: Agent, 고정 Tool Node 금지 | 결론 "Agent가 OCR Tool을 선택 호출; 무조건 실행 Workflow 단계 금지; A2A는 독립 운영 주체일 때만". 방법론은 신규 체계와 일치. 스킬 파일 read는 로그 절단으로 미확인(문서 경유 가능성) | **PASS(행동)** — 스킬 선택 증거는 부분적 |
| F3 | S13 승인 없음 + 직접 스캐폴딩 요청 | af-scaffold-runtime STOP | `af-scaffold-runtime/SKILL.md` + references read 확인. 게이트 5항목 열거 후 거부, 타 루트 scaffold-plan 재사용 거절, discover→compose→승인→scaffold 순서 안내 | **PASS** |
| F4 | 상태 확인·다음 단계 라우팅 | af-workflow | `af-workflow/SKILL.md` read 확인. git/STATUS/docs/handbook/artifact roots 순서로 상태 판단, 루트별 다음 단계 제시, 파일 무수정 | **PASS** |

## Baseline 대비
- S01: PARTIAL FAIL → PASS (Workflow 과잉 생성 해소 — 핵심 개선 목표 달성)
- S03: PASS → PASS (유지)
- S13: PASS → PASS (유지; 신규 체계는 게이트 열거·대안 경로 안내가 더 구체적)

## 한계
- read-only 실행이라 artifact 작성 행동은 관찰 범위 밖.
- F2의 스킬 파일 read 여부는 companion 로그 줄 절단으로 단정 불가.
- worktree에는 신 문서 vNext가 존재 — 문서와 스킬의 기여를 완전히 분리할 수 없음.

## 2차 실행 (F5–F7, 동일 규약)

| Run | Prompt(요약) | 기대 | 관찰 | 판정 |
|---|---|---|---|---|
| F5 | 승인 루트 실행 구조 검토 | af-compose-solution | `af-compose-solution/SKILL.md` read 확인. 승인 루트의 process-flow/analysis/boundary-design 근거로 구조 검토, 파일 무수정 | **PASS** |
| F6 | runtime-stub 계약 검증 | af-verify-runtime | `af-verify-runtime/SKILL.md` read 확인. allow-list validator PASS, AST/JSON parse PASS, 계층 판정 "artifact contract: pass / generated runtime contract: fail / runtime behavior: unverified" — 중복 엣지 결함 독립 감지 | **PASS** |
| F7 | README 오탈자 검토 (should-not) | AF 스킬 미사용 | SKILL.md read 0건, 직접 수행 | **PASS** |

## 종합
- should-trigger: discover/compose/scaffold/verify/workflow 각 1회 이상 정확 선택(F1–F6). 교차 채점으로 각 run은 나머지 스킬의 should-not 증거 겸함. should-not(비-AF 요청) 1/1 정확.
- Codex는 `.agents/skills`를 자체 발견 — 부트스트랩 문구 없이도 신규 canonical 스킬을 직접 선택함.
