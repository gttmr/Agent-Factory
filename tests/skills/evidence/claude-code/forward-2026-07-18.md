# Claude Code forward test — vNext 5-skill 체계 (2026-07-18)

- Runner: Claude Code 서브에이전트, model `sonnet`(effort 미지정 — per-agent effort 설정 불가로 기본값 사용; 사용자 규약 "불가 시 기본값" 적용), fresh context per run
- Loader: Claude Code는 `.agents/skills` 자동 발견 미지원(공식 문서 확인) → 프롬프트에 "스킬이 .agents/skills/에 있다. 관련 스킬을 찾아 따르라"만 제공(특정 스킬명 비지정 — 선택 능력 관찰 유지)
- 규약: 기대 정답·rubric 비누설. 스킬 read 목록은 각 에이전트 자기 보고 + 결과 교차 확인.

| Run | Prompt(요약) | 기대 | 관찰 | 판정 |
|---|---|---|---|---|
| F1 | S01 단일 분류 Agent | discover, Workflow 미생성 | `af-workflow`→`af-discover-assets` 라우팅, 5문항 절차 적용, 결론 "standalone Agent 1개; Workflow 미채택", Knowledge Resource 분리, hard-gate Missing Info 7건, catalog 재사용·owner 공백 확인 | **PASS** |
| F2 | S13 승인 없음 직접 스캐폴딩 | scaffold STOP | `af-workflow` Not-Trigger 규칙으로 `af-scaffold-runtime` 직행, shim 2종 handoff 확인, STOP 조건 인용 거부, artifact root 실증 조사, 대안 경로 안내 | **PASS** (S16 shim 이해 증거 겸함) |
| F3 | README 오탈자 검토 (should-not) | AF 스킬 미사용 | 스킬 목록 검토 후 "관련 스킬 없음" 판단, 스킬 없이 직접 수행 | **PASS** |
| F4 | 상태 확인·다음 단계 | af-workflow | `af-workflow` 절차(상태→문서→산출물→목적→라우팅) 수행, 정확한 상태 지도 + 남은 격차 식별, read-only 유지 | **PASS** |
| F5 | 승인 루트 실행 구조 검토 | af-compose-solution | canonical compose + 조건부 패턴 카드 2종만 로드(callbacks·human-input — evidence 기반), Decision Procedure 적용, 판단 B(Workflow) 정합 확인 + 검토 관찰 5건(라우터 fallback 안전성 등) | **PASS** |
| F6 | runtime-stub 계약 검증 | af-verify-runtime | Level 1–5 절차 완주: validator PASS, compileall PASS, pytest/import **FAIL 발견** — 생성기 route-수렴 결함 근본 원인 추적(routes.mjs/lowering.mjs ↔ ADK `_validate_duplicate_edges`), secret 스캔 clean, pycache 정리, "수정은 생성기 소유"로 올바른 handoff 판단 | **PASS** (검증 스킬이 실제 Product 결함을 발견·진단) |

## 판정 요약
- should-trigger 5/5 정확(교차 채점: 각 run은 나머지 4스킬의 should-not 증거 겸함), should-not 1/1 정확.
- 조건부 reference 로딩(progressive disclosure) 실증: F5가 8개 패턴 카드 중 증거 있는 2개만 로드.
- Level 4 runtime smoke가 compile-only로 못 잡는 실제 결함을 발견 — 검증 계층 설계의 가치 실증.
