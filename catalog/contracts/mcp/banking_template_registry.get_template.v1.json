{
  "schema_ref": "catalog.banking.template_registry.get_template.v1",
  "server": "banking-template-registry-mcp",
  "tool": "get_template",
  "title": "Banking message template registry",
  "description": "Synthetic contract for retrieving approved customer-facing template metadata.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["template_key"],
    "properties": {
      "template_key": { "type": "string" },
      "locale": { "type": "string" },
      "channel": { "type": "string" }
    }
  },
  "outputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["template", "required_variables", "approval_constraints"],
    "properties": {
      "template": { "type": "object" },
      "required_variables": {
        "type": "array",
        "items": { "type": "string" }
      },
      "approval_constraints": { "type": "object" }
    }
  },
  "success_examples": [
    {
      "name": "loan_document_followup_template",
      "arguments": {
        "template_key": "loan_document_followup_v1",
        "locale": "ko-KR",
        "channel": "internal_fixture"
      },
      "structuredContent": {
        "template": {
          "template_key": "loan_document_followup_v1",
          "body": "Synthetic fixture message: document follow-up required."
        },
        "required_variables": ["customer_token", "missing_documents"],
        "approval_constraints": {
          "human_review_required": true,
          "source": "synthetic_fixture"
        }
      }
    }
  ],
  "error_examples": [
    {
      "name": "unknown_template",
      "arguments": {
        "template_key": "unknown"
      },
      "isError": true,
      "message": "template_key is not registered"
    }
  ],
  "mock_response": {
    "content": [
      {
        "type": "text",
        "text": "{\"template_key\":\"loan_document_followup_v1\"}"
      }
    ],
    "structuredContent": {
      "template": {
        "template_key": "loan_document_followup_v1",
        "body": "Synthetic fixture message: document follow-up required."
      },
      "required_variables": ["customer_token", "missing_documents"],
      "approval_constraints": {
        "human_review_required": true,
        "source": "synthetic_fixture"
      }
    },
    "isError": false
  }
}
