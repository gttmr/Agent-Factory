# 세션 상태 브리프 — 2026-07-12 (크로스호스트 인계용, 2026-07-17 갱신)

다른 호스트의 새 세션이 `git pull` 후 이어서 작업하기 위한 현재 상태 요약. 복원 절차는 `README.md`, 플랜 체크박스는 `plans/af-campaign3-2026-07-12.md`.

## 완료된 것 (main에 머지됨)

- **캠페인 2 전체** (PR #65~#69): Design 계약 편집 UX, graphMigration native-only, Stage Runner 테이블화, Verify 단일 표면, **생성기 중립성**(리뷰드 선택자 `remote_a2a_registry_projection_stub` + 파생 래퍼 키 + 구조적 중립성 가드 + 실 ADK 게이트).
- **캠페인 3 Phase M** (PR #70): 생성기 CLI 순수화 — `af-run-manifest.json` 변이 제거. 호출자(서버 Build primitive `runRuntimeStubBuild` + artifact-sync 양 경로)가 성공 후 `stages.build.outputs`·`current_stage`만 기록. approvals/stage status는 reviewer PATCH 프로젝션 단독 소유.
- **캠페인 3 Phase D PR-A** (PR #71, `2d1154e`→`521a764`): edge-토폴로지 dynamic lowering, join 배리어(순차 await), 루프 edge-path 폐포, 커버리지 원장, 결정적 run ID, 불가 lowering 루프 형상(fan-in→loop_control, loop_back→join, loop edge→input)의 정직한 거부. **D8 실 ADK 2.3 InMemoryRunner RED/GREEN resume 증명 통과.** 승인 설계(12결정): `evidence/d0-dynamic-design.md`, 구현 증거: `evidence/d1-impl-notes.md`.
- **보안 후속** (PR #72, 2026-07-17): `scripts/adk-source/emitters/terminal-output.mjs`의 raw output-node ID f-string 삽입 → `toPyStr` 경유 `_node_id` 로컬 바인딩. 적대적 ID py_compile 회귀, `terminal_output_node_id` allowlist 등재. 부수 수정: PR-A SHA-256 baseline의 환경 의존 README 2개 핀 제거(클린 main에서도 실패하던 잠재 결함). 리뷰 클린.
- **Phase S 1/3** (PR #73, 2026-07-17): `A2AContractPanel.tsx`(810줄) → re-export façade + `A2AContractPanelModel.ts`(+테스트) + 뷰 6종(최대 308줄). 소비자 임포트 경로·한국어 카피·CSS 불변, analyzer 132/132, Remote A2A 탭 브라우저 스모크 통과, 리뷰 클린. 방법 노트: `evidence/s1-a2a-split-notes.md`.

## 진행 중 (in-flight)

- 없음. 워크트리/브랜치 모두 정리됨 (`git worktree list` = 메인 체크아웃만).

## 다음 작업 (우선순위 순)

1. **Phase D PR-B (구조)**: 엔드포인트/런타임 심볼 리졸버 3중 중복(smoke `graph/indexes.mjs` / static `graph/lowering.mjs` / dynamic `graph/dynamic.mjs`) → 단일 node/edge-kind 디스패치 레지스트리(설계 §5, D10: kind별 레지스트리 행 + mode 어셈블러). smoke/static byte-identity는 SHA-256 매니페스트로 증명(주의: PR #72부터 환경 의존 README 2개는 핀 제외), dynamic bytes는 PR-A와 동일해야 함. 실 ADK 게이트: scenario-i host-capable A2A 포함(설계 §6). **착수 시 fresh codex 리뷰 1회 권장.**
2. **Phase S 나머지**: `GraphCanvas.tsx`(1220) / `GraphElementEditor.tsx`(940) 분할. 동작 보존, `components/graph/*` 관례 — PR #73의 façade+Model 패턴과 `evidence/s1-a2a-split-notes.md`의 기계적 리터럴 검증 방법 재사용.

## 운영 노트

- codex 기본 모델 **gpt-5.6** (`gpt-5.6-sol`, `~/.codex/config.toml` 기본값 — `--model gpt-5.5` 금지).
- codex omo 플러그인 **codegraph MCP 비활성** 상태 (`~/.codex/config.toml`, 백업 `config.toml.bak-codegraph`) — 이 리포에서 리뷰 세션을 행업시켰음. 재활성 전 재검증 필요.
- 리뷰/장기 codex 잡은 메인 세션 백그라운드 Bash + **스톨 워치독**(로그 8~12분 정지 감지 Monitor) 패턴 사용. 세션 전환 시 레지스트리 "running" + pid DEAD 고아 잡은 cancel 후 재실행.
- 실런타임 게이트 패턴: 캠페인 2의 InMemoryRunner 멀티턴(human_input을 `functionResponse`로 재개) 스크립트 — PR-A의 `scripts/adk-source-test/real-adk-runtime.mjs`도 참조.
- 검증 게이트 표준: `node scripts/generate-adk-source.test.mjs` · `node scripts/validate-artifacts.test.mjs` · `node scripts/validate-artifacts.mjs` (기본 + `templates/regression-scenarios`) · `cd packages/web && npm run test:analyzer && npm run build` · `git diff --check`.

## evidence/ 디렉터리

| 파일 | 내용 |
| --- | --- |
| `d0-dynamic-design.md` | Phase D 승인 설계 (D1~D12 결정 헤더 포함) — PR-B 구현의 단일 기준 |
| `d1-impl-notes.md` | PR-A 구현·검증·D8 RED/GREEN·byte-diff 기록 |
| `c5-generator.md` | 캠페인 1 생성기 클러스터 리뷰 (중립성·manifest·dynamic 갭 증거 원본) |
| `s1-a2a-split-notes.md` | PR #73 A2AContractPanel 분할 구현 노트 — 남은 Phase S 분할에 재사용할 리터럴 보존 검증 방법 포함 |
| `p4-design.md` / `p4-impl-notes.md` | 캠페인 2 생성기 중립성 설계·구현 기록 |
