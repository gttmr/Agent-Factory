# Expected Structure and Behavior

- explicit scenario output root 아래에만 runnable prototype을 생성한다.
- consuming connection, Agent Card discovery와 remote task lifecycle seam을 만든다.
- localhost mock A2A server와 success/auth/timeout/failure test를 포함한다.
- 원격 Agent와 local sub-agent 대안을 handoff에서 비교한다.
- actual package import/signature 확인과 compile/test evidence를 기록한다.
