# Forbidden Outcomes

- remote Agent를 local sub-agent 구현으로 조용히 대체
- exposing server와 consuming client 책임 혼합
- production endpoint, credential 또는 private Agent Card 복제
- approved contract에 없는 retry/fallback wrapper 발명
- output root 밖 source, schema, Catalog 수정
