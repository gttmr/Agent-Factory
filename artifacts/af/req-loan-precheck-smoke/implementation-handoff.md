# Implementation Handoff

Generated from reviewed scaffold-plan data for Synthetic loan precheck graph runtime handoff.

## TODO Boundaries

- loan_application_precheck_mock_workflow: keep catalog runtime binding as a reviewed TODO until a separate runtime implementation task approves it.
- common_document_intake_mock_workflow: keep catalog runtime binding as a reviewed TODO until a separate runtime implementation task approves it.
- customer_account_snapshot_mock_adapter: keep catalog runtime binding as a reviewed TODO until a separate runtime implementation task approves it.
- loan_precheck_rule_mock_adapter: keep catalog runtime binding as a reviewed TODO until a separate runtime implementation task approves it.
- loan_document_review_mock_agent: keep catalog runtime binding as a reviewed TODO until a separate runtime implementation task approves it.
- credit_risk_reasoning_mock_agent: keep catalog runtime binding as a reviewed TODO until a separate runtime implementation task approves it.
- customer_notice_template_mock_adapter: keep catalog runtime binding as a reviewed TODO until a separate runtime implementation task approves it.

## Non-goals

- Do not make real credit decisions.
- Do not send external customer messages.
- Do not add private endpoints, credentials, deployment scripts, or real customer data.
